import 'package:flutter/material.dart';

class SoloPlay extends StatelessWidget {
  const SoloPlay({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Solo Play Mode'),
    );
  }
}